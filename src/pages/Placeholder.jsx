export default function Placeholder({ title }) {
  return (
    <div className="page">
      <h1 className="page-title">{title}</h1>
      <p className="page-sub">Cette section arrive bientôt.</p>
    </div>
  );
}
